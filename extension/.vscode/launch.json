{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run Pintos Test Explorer",
      "type": "extensionHost",
      "request": "launch",
      "args": [
        "--extensionDevelopmentPath=${workspaceFolder}",
        "${workspaceFolder}/.."
      ]
    }
  ]
}
